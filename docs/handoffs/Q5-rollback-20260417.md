## Slice
Rollback Q5 subscription-path redesign work to restore the last known good post-Q7 behavior.

## Changed
- Reverted the Q5 subscription-path commits that introduced config-loading entrypoint changes, the subscription snapshot split, the legacy config backfill, and the stable-entry ordering change.
- Restored `docs/NEXT_SLICE_QUEUE.md` to the post-Q7 state where Q5 remains blocked behind the `/sub` contract boundary.
- Left unrelated local changes such as `docs/SYSTEM_DESIGN.md` untouched.

## Release Marker
- Commit: pending
- Tag: pending

## Verified
- `node --loader ./tests/loader.mjs --test tests/worker.test.js`
- `node --test tests/security.test.js`
- `node --test tests/admin_compatibility.test.js`

## Risks / Open Questions
- This returns the repository to the last known good code path after Q7, but the user-reported iOS import failure still needs a fresh root-cause analysis from that baseline.

## Next Slice
- Reproduce the iOS import failure from the rolled-back baseline and isolate whether it comes from the public deployment path, subscription payload shape, or a client-specific transport expectation before attempting another `/sub` change.
