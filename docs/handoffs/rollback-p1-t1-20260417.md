# Handoff: Rollback P1-T1 Config Loading Contract

## Slice

- Task ID: rollback-P1-T1
- Lane: A
- Objective completed: restore the pre-P1-T1 runtime behavior after a user-reported regression

## Changed

- `_worker.js` - removed the experimental split between hot-path config loading and admin enrichment
- `tests/worker.test.js` - removed the regression test that depended on the experimental export
- `docs/IMPLEMENTATION_PLAN.md` - marked P1-T1 as reverted pending redesign
- `docs/handoffs/P1-T1-config-loading-contract.md` - marked as superseded by rollback

## Release Marker

- Commit:
- Tag:

## Verified

- `node --loader ./tests/loader.mjs --test tests/worker.test.js` - pass
- `node --test tests/security.test.js` - pass
- `node tests/test_sha224.js` - pass
- Manual checks:
  - confirmed `_worker.js` no longer contains the added config-loading split or test-only export

## Intentionally Not Touched

- `AGENTS.md` collaboration rules
- git tag discipline
- context pack and ADR documents

## Risks / Open Questions

- the exact production failure mode still needs postmortem evidence from runtime logs or user-visible symptoms
- P1-T1 should not be retried until we can reproduce the Cloudflare-side regression more directly

## Next Slice

- Recommended next task: analyze production-specific regression cause before attempting another hot-path refactor
- Recommended owner lane: A with C support
