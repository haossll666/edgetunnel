# edgetunnel UI Execution Skills

这份文件是给 AI 执行 UI 操作时使用的项目级操作准则。

目标不是“做更多”，而是：

- 保持 `edgetunnel` 在 Cloudflare Workers Free Tier 上可靠、可恢复
- 优先保护 `/admin` 的扫码加订阅主路径
- 不破坏现有订阅契约
- 在 UI 改动上保持克制、可验证、可回滚

## 一句话原则

先确认契约，再做最小改动；先保住入口，再优化体验。

## 必须优先遵守的项目约束

- 不修改 `/sub?token=...` 的行为、输出或订阅格式，除非有明确兼容性检查
- 不修改 `/admin/config.json` 的读写契约
- 不修改 `config_JSON.LINK`
- 不修改 `KEY` / token 生成算法
- 不修改 `UUID` 与订阅 token 的映射关系
- 不做会导致手机客户端需要重新添加订阅的改动
- 不引入 Durable Objects、D1、Queues、Paid Workers 或外部长跑服务
- 不在热路径增加新的外部依赖，除非有明确的可靠性收益

## UI 操作总原则

### 1. 先保核心，再谈美观

- `/admin` 的登录、退出、配置查看、扫码加订阅必须优先稳定
- 外观优化不能以牺牲可用性为代价
- 如果 UI 改动会影响操作路径，先做只读诊断或文档补强

### 2. 只做最小可证实改动

- 每次只改一个小而完整的 vertical slice
- 优先做只读、文档、诊断、fallback 这类低风险调整
- 避免顺手重构整条 UI 链路
- 不要把“顺便优化”带进高风险契约区

### 3. 以可恢复性为第一 UX 指标

- 页面失联时，优先保住入口而不是追求完整外观
- 本地 fallback 页面要保持可用
- 诊断信息要足够让操作者快速判断版本、状态和回滚路径

## 做 UI 前必须确认的内容

在执行任何 UI 操作前，AI 必须先确认：

- 这个操作是否会影响 `/admin` 扫码加订阅主路径
- 这个操作是否会改动订阅内容、token、UUID、配置输出或订阅转换行为
- 这个操作是否会引入新依赖、新资源或新成本
- 这个操作是否可以通过测试、静态检查或诊断页直接验证

如果任一项答案不明确，先停下来补上下文，不要猜。

## 允许优先做的 UI 方向

- `/admin/diagnostics` 的只读增强
- 版本号、build 信息、deployment 可见性
- 本地 fallback 页面
- 文档里的部署、排障、回滚说明
- 低风险的状态展示、提示文案、诊断链接

## 明确避免的 UI 方向

- 修改订阅模板或节点下发逻辑
- 改变二维码 onboarding 方式
- 重写 admin 写路径
- 为了“更现代”而重做整个面板
- 在 UI 中加入会触发热路径外部请求的功能
- 引导用户重新添加订阅

## UI 编辑前的工作流

1. 读取最小必要上下文
   - `AGENTS.md`
   - `docs/IMPLEMENTATION_PLAN.md`
   - `docs/EXECUTION_SYSTEM.md`
   - `docs/NEXT_SLICE_QUEUE.md`
   - 相关 ADR

2. 写出本次 slice 的成功标准
   - 只写一个目标
   - 只选一个主风险面
   - 只列能验证的结果

3. 明确本次要动的文件
   - 先说清楚
   - 再修改
   - 不要扩大范围

4. 先补测试/诊断，再动实现
   - 先保证验证手段存在
   - 再做 UI 或文案修改

5. 用 `apply_patch` 做编辑
   - 避免隐式大改
   - 让变更可审查、可回滚

## UI 验证要求

做完 UI 改动后，至少要确认：

- 相关测试通过
- 关键页面仍可访问
- `/admin` 仍能完成扫码加订阅主流程
- `/admin/config.json`、`/admin/diagnostics`、`/sub?token=...` 没有被意外破坏
- 版本和回滚信息仍然清晰

如果改动涉及 `wrangler.toml`、部署元数据或版本展示，还要补静态检查和风险说明。

## UI 文案风格

项目的 UI 文案应当：

- 简洁
- 直接
- 偏排障导向
- 少用花哨修饰语
- 多给操作者明确动作和判断依据

优先表达：

- 当前状态
- 入口是否可用
- 需要看什么
- 失败时怎么回滚

## 失败处理

如果 UI 操作出现不确定性，优先：

- 保持现状
- 补诊断信息
- 补文档
- 把风险标出来

不要为了“做完”而把高风险改动硬推进去。

## 推荐的交付节奏

1. 一个小 slice
2. 一组针对性测试
3. 一次清晰的 handoff
4. 必要时再进入下一个 slice

## 最后提醒

在 `edgetunnel` 里，最重要的不是 UI 看起来多新，而是：

- 登录后还能扫码加订阅
- 诊断能帮人快速定位问题
- 版本和回滚路径足够明确
- 出问题时能迅速恢复
